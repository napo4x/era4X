@CHARACTER_EDIT_UI_ABL(対象)
#DIM 対象
LOCAL:1 = 0
FOR LOCAL, 0, VARSIZE("ABL")
	SIF ABLNAME:(LOCAL) == ""
		CONTINUE
	PRINTBUTTON @"% "[" + ABLNAME:LOCAL + "]" , 10, LEFT%:", LOCAL

	IF GROUPMATCH(LOCAL, GETNUM(ABL, "白兵"), GETNUM(ABL, "操艦"), GETNUM(ABL, "知略"), GETNUM(ABL, "政治"), GETNUM(ABL, "料理"), GETNUM(ABL, "歌唱"), GETNUM(ABL, "超能力"), GETNUM(ABL, "野心"))
		CALL PRINT_ALPHABET_RANK(ランク_ＳＬＧ, ABL:対象:LOCAL)
	ELSEIF LOCAL == GETNUM(ABL, "性知識")
		CALL PRINT_ALPHABET_RANK(ランク_性知識, ABL:対象:LOCAL)
	ELSE
		CALL PRINT_ALPHABET_RANK(ランク_その他, ABL:対象:LOCAL)
	ENDIF

	PRINTFORM  {ABL:対象:LOCAL, 4, RIGHT} 
	LOCAL:1 ++
	SIF LOCAL:1 != 0 && LOCAL:1 % 5 == 0
		PRINTL
NEXT
PRINTL
	PRINTBUTTON "[経験を元に自動レベルアップ]", 200
PRINTL
PRINTL

@CHARACTER_EDIT_INPUT_ABL(対象, 入力値)
#DIM 対象
#DIM 入力値

IF 入力値 == 200
	FOR LOCAL, 0, VARSIZE("ABL")
		CALL TRAIN_AUTO_ABLUP(対象, 0)
	NEXT
	PRINTW 完了
	RETURN 0
ENDIF

SIF !INRANGE(入力値, 0, VARSIZE("ABL") - 1) || ABLNAME:入力値 == ""
	RETURN 0

PRINTFORML 値を入力 現在値{ABL:対象:入力値} 
PRINTFORML (%ABLNAME:入力値%を編集中 {ABL_MIN:入力値}～{ABL_MAX:入力値})

INPUT

RESULT = LIMIT(RESULT, ABL_MIN:入力値, ABL_MAX:入力値)

ABL:対象:入力値 = RESULT

SIF ABL:対象:超能力 > 0
	TALENT:対象:超能力者 = 1

