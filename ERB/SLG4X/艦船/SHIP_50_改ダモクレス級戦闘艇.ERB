;------------------------
; 改ダモクレス級戦闘艇
;------------------------
@GET_SHIP_NAME_50
RESULTS = 改ダモクレス級戦闘艇

@GET_SHIP_STATUS_50(STATUS)
#DIM REF STATUS

STATUS:CATEGORY = コルベット
STATUS:SHIP_HP = 400
STATUS:SHIP_AVOID = 15
STATUS:SHIP_MAIN_WEAPON_SIZE = 1
STATUS:SHIP_SUB_WEAPON_SIZE = 1
STATUS:SHIP_SIELD_SIZE = 1
STATUS:SHIP_ARMOR_SIZE = 1
STATUS:SHIP_ENGIN_SIZE = 1
STATUS:SHIP_PRICE = 1800000
STATUS:SHIP_CREW = 1

STATUS:SHIP_MAIN_WEAPON_SLOT = 1
STATUS:SHIP_SUB_WEAPON_SLOT = 1
STATUS:SHIP_SIELD_SLOT = 1
STATUS:SHIP_ARMOR_SLOT = 0
STATUS:SHIP_OPTION_SLOT = 0
STATUS:SHIP_CRITICAL_RISK = 30
STATUS:SHIP_VARIANT = 特殊分類_通常艦艇


;CALL SHIP_PRINTFORML_MESSAGE("(説明文が入ります)", (文字色を変更する場合にはカラーコードを指定してください), 動作モード) 
;------------------------
@GET_SHIP_DESCRIPTION_F_50(動作モード = 0)
#DIM 動作モード

CALL SHIP_PRINTFORML_MESSAGE("", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("企業連合製の単座式大型戦闘艇。", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("搭乗者の生命財産を軽視していたダモクレス級を、真っ当な人間向けに再設計した機体。", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("戦闘に耐えられるエネルギーシールドの運用能力を付与し、副砲に相当するハードポイントを追加。", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("火力のみでいえばコルベットそのものである。", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("しかし総合的な能力では大型戦闘艇の枠を出ることはなく、", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("本格的な艦隊戦にて有効打を期待するには、数を大量にそろえざるをえないだろう。", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("あくまでも民間警備会社や傭兵向けの、やや強力な装備でしかないことは認識するべきだ。", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("企業連合はこの機体を輸出して利益を上げようとしているが、", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("元になったダモクレス級の印象があまりにも悪すぎ、目下苦戦中である。", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("", , 動作モード)


@GET_SHIP_RESOURCE_50(USE_RESOURCE)
#DIM REF USE_RESOURCE

USE_RESOURCE:武装合金 = 30
USE_RESOURCE:装甲合金 = 15
USE_RESOURCE:シールド発生装置 = 30
USE_RESOURCE:武装制御機器 = 30
USE_RESOURCE:合金加工剤 = 25
USE_RESOURCE:耐熱合金 = 10
USE_RESOURCE:超電導回路 = 30
USE_RESOURCE:タイラーセンサー = 20


@SET_SHIP_PRESET_50(SHIP_ID)
#DIM SHIP_ID

SHIP_EQUIP:主砲:0:SHIP_ID = 1
SHIP_EQUIP:副砲:0:SHIP_ID = 1
SHIP_EQUIP:シールド:0:SHIP_ID = 1
SHIP_EQUIP:エンジン:0:SHIP_ID = 1
SHIP_EQUIP:AI:0:SHIP_ID = 1
SHIP_EQUIP:センサー:0:SHIP_ID = 1


@IS_SALE_SHIP_50(CAN_SALE_SHIP)
#DIM REF CAN_SALE_SHIP

CAN_SALE_SHIP:必要好感度 = 50

SETBIT CAN_SALE_SHIP:建造可能国家, 国家ID_企業連合 + 国家ID_ビット演算ヘッダー調整
RETURN 0


; 販売条件のテキストをRESULTSに,それを表示してもいい場合は1を、表示不要な場合は0を返却
@TERM_OF_SALE_50
RETURN 0

; @CAN_SPACECRAFT_CARRYING_50が存在しません

; (非定型的な)コメント文はそのままだと処理するどころか認識すらできないので定型処理できる形式に落とし込む為の関数です。
; 販売条件等で非定型的な処理をしている場合にはここで明言してあると(出力関数に見逃された場合にも)気が付ける可能性が増加します。
@GET_SHIP_COMENT_50(動作モード = 0)
#DIM 動作モード


