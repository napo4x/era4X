;------------------------
; ヘイロタイ型作業機
;------------------------
@GET_SHIP_NAME_125
RESULTS = ヘイロタイ型作業機

@GET_SHIP_STATUS_125(STATUS)
#DIM REF STATUS

STATUS:CATEGORY = 作業船
STATUS:SHIP_HP = 100
STATUS:SHIP_AVOID = 0
STATUS:SHIP_MAIN_WEAPON_SIZE = 1
STATUS:SHIP_SUB_WEAPON_SIZE = 1
STATUS:SHIP_SIELD_SIZE = 1
STATUS:SHIP_ARMOR_SIZE = 1
STATUS:SHIP_ENGIN_SIZE = 1
STATUS:SHIP_PRICE = 600000
STATUS:SHIP_CREW = 2

STATUS:SHIP_MAIN_WEAPON_SLOT = 1
STATUS:SHIP_SUB_WEAPON_SLOT = 1
STATUS:SHIP_SIELD_SLOT = 0
STATUS:SHIP_ARMOR_SLOT = 1
STATUS:SHIP_OPTION_SLOT = 0
STATUS:SHIP_CRITICAL_RISK = 0
STATUS:SHIP_VARIANT = 特殊分類_通常艦艇


;CALL SHIP_PRINTFORML_MESSAGE("(説明文が入ります)", (文字色を変更する場合にはカラーコードを指定してください), 動作モード) 
;------------------------
@GET_SHIP_DESCRIPTION_F_125(動作モード = 0)
#DIM 動作モード

CALL SHIP_PRINTFORML_MESSAGE("ハースバルズ王国製の作業機。", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("スラム街などの再開発を進めるために、『平民向けの廉価な大型作業用機材』という名目で開発された。", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("…が、仕様面では『機材統一の観点から、重力下と無重力下の両方で運用が可能』", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("という無茶振りを要求され、何らかの作業中に過激派に奪取され、", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("農民の反乱に使われることを危惧した貴族の圧力で最新技術の使用が禁止された事もあり開発は難航。", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("最終的にはモノサイクルの左右から作業用アームが飛び出しているような珍妙な外見の機体に仕上がった。", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("運用試験では重力下でこそそれなりの性能を発揮したが、", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("無重力下では航続距離・姿勢制御・居住性・安全性と多岐に渡る問題点が発覚。", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("一度は廃案となりかけたが、規格外の廉価さが評価されて首の皮一枚繋がる形で採用が決定された。", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("また、宙域航行が可能な機材としては破格と言える安価さを誇るものの、", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("前出の通り航行性能と居住性は劣悪そのものであることから、", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("この機体に乗って脱走を試みた事例は確認されていない。", , 動作モード)


@GET_SHIP_RESOURCE_125(USE_RESOURCE)
#DIM REF USE_RESOURCE

USE_RESOURCE:武装合金 = 10
USE_RESOURCE:装甲合金 = 25
USE_RESOURCE:合金加工剤 = 5
USE_RESOURCE:耐熱合金 = 10
USE_RESOURCE:超電導回路 = 5


@SET_SHIP_PRESET_125(SHIP_ID)
#DIM SHIP_ID

SHIP_EQUIP:主砲:0:SHIP_ID = 67
SHIP_EQUIP:副砲:0:SHIP_ID = 86
SHIP_EQUIP:装甲:0:SHIP_ID = 92
SHIP_EQUIP:エンジン:0:SHIP_ID = 18
SHIP_EQUIP:AI:0:SHIP_ID = 1


@IS_SALE_SHIP_125(CAN_SALE_SHIP)
#DIM REF CAN_SALE_SHIP

CAN_SALE_SHIP:必要好感度 = 0

SETBIT CAN_SALE_SHIP:建造可能国家, 国家ID_ハールバルズ + 国家ID_ビット演算ヘッダー調整
RETURN 0


; 販売条件のテキストをRESULTSに,それを表示してもいい場合は1を、表示不要な場合は0を返却
@TERM_OF_SALE_125
RETURN 0

; @CAN_SPACECRAFT_CARRYING_125が存在しません

; (非定型的な)コメント文はそのままだと処理するどころか認識すらできないので定型処理できる形式に落とし込む為の関数です。
; 販売条件等で非定型的な処理をしている場合にはここで明言してあると(出力関数に見逃された場合にも)気が付ける可能性が増加します。
@GET_SHIP_COMENT_125(動作モード = 0)
#DIM 動作モード

CALL SHIP_PRINTFORML_MESSAGE("SHIP_EQUIP:センサー:0:SHIP_ID = 0;目視確認のみ", , 動作モード)

