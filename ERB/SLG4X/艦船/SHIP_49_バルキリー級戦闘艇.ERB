;------------------------
; バルキリー級戦闘艇
;------------------------
@GET_SHIP_NAME_49
RESULTS = バルキリー級戦闘艇

@GET_SHIP_STATUS_49(STATUS)
#DIM REF STATUS

STATUS:CATEGORY = コルベット
STATUS:SHIP_HP = 100
STATUS:SHIP_AVOID = 30
STATUS:SHIP_MAIN_WEAPON_SIZE = 1
STATUS:SHIP_SUB_WEAPON_SIZE = 1
STATUS:SHIP_SIELD_SIZE = 1
STATUS:SHIP_ARMOR_SIZE = 1
STATUS:SHIP_ENGIN_SIZE = 1
STATUS:SHIP_PRICE = 艦船価格_自動決定
STATUS:SHIP_CREW = 6

STATUS:SHIP_MAIN_WEAPON_SLOT = 1
STATUS:SHIP_SUB_WEAPON_SLOT = 0
STATUS:SHIP_SIELD_SLOT = 1
STATUS:SHIP_ARMOR_SLOT = 0
STATUS:SHIP_OPTION_SLOT = 1
STATUS:SHIP_CRITICAL_RISK = 60
STATUS:SHIP_VARIANT = 特殊分類_通常艦艇


;CALL SHIP_PRINTFORML_MESSAGE("(説明文が入ります)", (文字色を変更する場合にはカラーコードを指定してください), 動作モード) 
;------------------------
@GET_SHIP_DESCRIPTION_F_49(動作モード = 0)
#DIM 動作モード

CALL SHIP_PRINTFORML_MESSAGE("", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("機械文明で作られた単座式大型戦闘艇。", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("なのだが、内部にドローン格納スペースが設けられており、", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("小型移乗コンテナを追加することで強襲揚陸艇として機能するようになっている。", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("この機体の目的とするところは明らかにそれだ。", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("だが、それと無関係であろうとも、複数人が生活可能な船室がついているということであり、", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("個人向けの護身用船舶、兼、自宅として一般に販売されている。", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("単座式大型戦闘艇としては高い機動性があり、それをオプションで拡張できもするが、引き換えに脆い。", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("", , 動作モード)


@GET_SHIP_RESOURCE_49(USE_RESOURCE)
#DIM REF USE_RESOURCE

USE_RESOURCE:武装合金 = 30
USE_RESOURCE:装甲合金 = 20
USE_RESOURCE:シールド発生装置 = 30
USE_RESOURCE:武装制御機器 = 20
USE_RESOURCE:合金加工剤 = 30
USE_RESOURCE:耐熱合金 = 30
USE_RESOURCE:超電導回路 = 20
USE_RESOURCE:タイラーセンサー = 20


@SET_SHIP_PRESET_49(SHIP_ID)
#DIM SHIP_ID

SHIP_EQUIP:主砲:0:SHIP_ID = 1
SHIP_EQUIP:シールド:0:SHIP_ID = 1
SHIP_EQUIP:エンジン:0:SHIP_ID = 1
SHIP_EQUIP:AI:0:SHIP_ID = 1
SHIP_EQUIP:センサー:0:SHIP_ID = 1


@IS_SALE_SHIP_49(CAN_SALE_SHIP)
#DIM REF CAN_SALE_SHIP

CAN_SALE_SHIP:必要好感度 = 0

SETBIT CAN_SALE_SHIP:建造可能国家, 国家ID_機械文明 + 国家ID_ビット演算ヘッダー調整
RETURN 0


; 販売条件のテキストをRESULTSに,それを表示してもいい場合は1を、表示不要な場合は0を返却
@TERM_OF_SALE_49
RETURN 0

; @CAN_SPACECRAFT_CARRYING_49が存在しません

; (非定型的な)コメント文はそのままだと処理するどころか認識すらできないので定型処理できる形式に落とし込む為の関数です。
; 販売条件等で非定型的な処理をしている場合にはここで明言してあると(出力関数に見逃された場合にも)気が付ける可能性が増加します。
@GET_SHIP_COMENT_49(動作モード = 0)
#DIM 動作モード


