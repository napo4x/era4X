;------------------------
; ウィッカーマン戦列甲冑
;------------------------
@GET_SHIP_NAME_51
RESULTS = ウィッカーマン戦列甲冑

@GET_SHIP_STATUS_51(STATUS)
#DIM REF STATUS

STATUS:CATEGORY = コルベット
STATUS:SHIP_HP = 25
STATUS:SHIP_AVOID = 45
STATUS:SHIP_MAIN_WEAPON_SIZE = 1
STATUS:SHIP_SUB_WEAPON_SIZE = 1
STATUS:SHIP_SIELD_SIZE = 1
STATUS:SHIP_ARMOR_SIZE = 1
STATUS:SHIP_ENGIN_SIZE = 1
STATUS:SHIP_PRICE = 艦船価格_自動決定
STATUS:SHIP_CREW = 1

STATUS:SHIP_MAIN_WEAPON_SLOT = 1
STATUS:SHIP_SUB_WEAPON_SLOT = 1
STATUS:SHIP_SIELD_SLOT = 1
STATUS:SHIP_ARMOR_SLOT = 1
STATUS:SHIP_OPTION_SLOT = 1
STATUS:SHIP_CRITICAL_RISK = 90
STATUS:SHIP_VARIANT = 特殊分類_メック


;CALL SHIP_PRINTFORML_MESSAGE("(説明文が入ります)", (文字色を変更する場合にはカラーコードを指定してください), 動作モード) 
;------------------------
@GET_SHIP_DESCRIPTION_F_51(動作モード = 0)
#DIM 動作モード

CALL SHIP_PRINTFORML_MESSAGE("ハールバルズ王国が開発した、大型機動歩兵にあたる機体。", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("本来、機動歩兵として開発したものではなく、高機動低コストの単座式戦闘艇として開発された。", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("豊秋津皇国の水雷戦隊に煮え湯を飲まされた一戦の反省として、魚雷によるジャイアントキリングを", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("確実に防ぐことを求められた王国軍は、宙雷艇を撃沈するに十分な攻撃力に、宙雷艇を追い立てられる機動性、", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("さらには損耗したところですぐに取返しがつく量産性を併せ持った兵器を求め、その解答がこの機体である。", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("試作機の段階では球状の船体を８つのアームから伸びたブースターで制御、推進するタコのような機体だったが、", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("こんな複雑怪奇な機体を直感的に扱える人間などおらず、理論上はコルベットを圧倒する機動性をまるで発揮できなかった。", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("しかも稼働部位のあまりの多さから機体剛性を確保できず、コストも高沸。本末転倒の有様だった。", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("これらの欠点を潰していった結果として、図らずも人型に近い可動構造に落ち着いたのである。", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("多少マシにはなったものの、機体そのものの防御力は宙間戦闘兵器としては貧弱な限りで、", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("シールドを削りきられれば死の危機がすぐそばに迫るが、当機の存在意義上、許容できる欠点とみなされている。", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("王国にて当機体に乗るのはほぼ例外なく平民出身の士官だが、同時に機体に紐づいた騎士爵が与えられる。", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("機体そのものは安くとも、騎士爵は決して安くはないのだ。たとえそれが、乱発されるものであっても。", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("価格に見合う性能を求めるのなら、他をあたろう。", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("", , 動作モード)


@GET_SHIP_RESOURCE_51(USE_RESOURCE)
#DIM REF USE_RESOURCE

USE_RESOURCE:武装合金 = 55
USE_RESOURCE:装甲合金 = 25
USE_RESOURCE:シールド発生装置 = 30
USE_RESOURCE:武装制御機器 = 25
USE_RESOURCE:合金加工剤 = 45
USE_RESOURCE:耐熱合金 = 25
USE_RESOURCE:超電導回路 = 30
USE_RESOURCE:タイラーセンサー = 20


@SET_SHIP_PRESET_51(SHIP_ID)
#DIM SHIP_ID

SHIP_EQUIP:主砲:0:SHIP_ID = 40
SHIP_EQUIP:副砲:0:SHIP_ID = 1
SHIP_EQUIP:シールド:0:SHIP_ID = 1
SHIP_EQUIP:装甲:0:SHIP_ID = 1
SHIP_EQUIP:エンジン:0:SHIP_ID = 1
SHIP_EQUIP:AI:0:SHIP_ID = 1
SHIP_EQUIP:センサー:0:SHIP_ID = 1


@IS_SALE_SHIP_51(CAN_SALE_SHIP)
#DIM REF CAN_SALE_SHIP

CAN_SALE_SHIP:必要好感度 = 600

SETBIT CAN_SALE_SHIP:建造可能国家, 国家ID_ハールバルズ + 国家ID_ビット演算ヘッダー調整
RETURN 0


; 販売条件のテキストをRESULTSに,それを表示してもいい場合は1を、表示不要な場合は0を返却
@TERM_OF_SALE_51
RETURN 0

; @CAN_SPACECRAFT_CARRYING_51が存在しません

; (非定型的な)コメント文はそのままだと処理するどころか認識すらできないので定型処理できる形式に落とし込む為の関数です。
; 販売条件等で非定型的な処理をしている場合にはここで明言してあると(出力関数に見逃された場合にも)気が付ける可能性が増加します。
@GET_SHIP_COMENT_51(動作モード = 0)
#DIM 動作モード


