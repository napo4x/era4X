;------------------------
; 下級汎用独立モジュール
;------------------------
@GET_SHIP_NAME_145
RESULTS = 下級汎用独立モジュール

@GET_SHIP_STATUS_145(STATUS)
#DIM REF STATUS

STATUS:CATEGORY = 建造物
STATUS:SHIP_HP = 2000
STATUS:SHIP_AVOID = 0
STATUS:SHIP_MAIN_WEAPON_SIZE = 4
STATUS:SHIP_SUB_WEAPON_SIZE = 4
STATUS:SHIP_SIELD_SIZE = 4
STATUS:SHIP_ARMOR_SIZE = 4
STATUS:SHIP_ENGIN_SIZE = 4
STATUS:SHIP_PRICE = 0
STATUS:SHIP_CREW = 0

STATUS:SHIP_MAIN_WEAPON_SLOT = 2
STATUS:SHIP_SUB_WEAPON_SLOT = 2
STATUS:SHIP_SIELD_SLOT = 2
STATUS:SHIP_ARMOR_SLOT = 2
STATUS:SHIP_OPTION_SLOT = 1
STATUS:SHIP_CRITICAL_RISK = 0
STATUS:SHIP_VARIANT = 特殊分類_建造物


;CALL SHIP_PRINTFORML_MESSAGE("(説明文が入ります)", (文字色を変更する場合にはカラーコードを指定してください), 動作モード) 
;------------------------
@GET_SHIP_DESCRIPTION_F_145(動作モード = 0)
#DIM 動作モード

CALL SHIP_PRINTFORML_MESSAGE("迎撃戦で敵軍を迎え撃つために開発された多目的汎用独立モジュール", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("モジュールの名の通り宇宙港のモジュールとして開発されたが独立していたほうが便利なため再設計された", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("下級の名は旧型であることを示すが戦闘能力は現代戦闘にも十分通じる", , 動作モード)
CALL SHIP_PRINTFORML_MESSAGE("", , 動作モード)


@GET_SHIP_RESOURCE_145(USE_RESOURCE)
#DIM REF USE_RESOURCE

USE_RESOURCE:武装合金 = 2400
USE_RESOURCE:装甲合金 = 2400
USE_RESOURCE:シールド発生装置 = 2400
USE_RESOURCE:武装制御機器 = 2400
USE_RESOURCE:合金加工剤 = 3000
USE_RESOURCE:耐熱合金 = 1200
USE_RESOURCE:超電導回路 = 200
USE_RESOURCE:タイラーセンサー = 50


@SET_SHIP_PRESET_145(SHIP_ID)
#DIM SHIP_ID



@IS_SALE_SHIP_145(CAN_SALE_SHIP)
#DIM REF CAN_SALE_SHIP

RETURN 0


; 販売条件のテキストをRESULTSに,それを表示してもいい場合は1を、表示不要な場合は0を返却
@TERM_OF_SALE_145
RETURN 0

; (非定型的な)コメント文はそのままだと処理するどころか認識すらできないので定型処理できる形式に落とし込む為の関数です。
; 販売条件等で非定型的な処理をしている場合にはここで明言してあると(出力関数に見逃された場合にも)気が付ける可能性が増加します。
@GET_SHIP_COMENT_145(動作モード = 0)
#DIM 動作モード


