;-----------------------------------
;基本値計算に先んじて処理するもの
;-----------------------------------
@SKILL_0_PERSONAL_40_EXIST
RETURN 1

@SKILL_0_PERSONAL_40_NAME
RESULTS = 大型艦専門

;レベルは1-5まで
@SKILL_0_PERSONAL_40_LEVEL
RETURN 2

@SKILL_0_PERSONAL_40_EXPLANATION
RESULTS = 自艦が戦艦か空母の場合、命中率+10\%、被ダメージ-10\%

;効果をここに記述
@SKILL_0_PERSONAL_40_EFFECT(VALUE, TYPE, CLASS, CHARA_ID)
#DIM VALUE
#DIM TYPE
#DIM CLASS
#DIM CHARA_ID
#DIM STATUS, STATUS_LENGTH

;実は戦艦や空母以外でも艦載機を使用出来れば命中ボーナスだけ受け取れる。
IF TYPE == TYPE_命中 && (CLASS == 戦艦 || CLASS == 空母 || CLASS == 艦種_航空機)
	TIMES VALUE, 1.10
ELSEIF TYPE == TYPE_被ダメージ && (CLASS == 戦艦 || CLASS == 空母)
	TIMES VALUE, 0.90
ENDIF
RETURN VALUE

@SKILL_0_PERSONAL_40_RATE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
RETURN 1000
