;-----------------------------------
;基本値計算に先んじて処理するもの
;-----------------------------------
@SKILL_0_PERSONAL_39_EXIST
RETURN 1

@SKILL_0_PERSONAL_39_NAME
RESULTS = 中型艦専門

;レベルは1-5まで
@SKILL_0_PERSONAL_39_LEVEL
RETURN 2

@SKILL_0_PERSONAL_39_EXPLANATION
RESULTS = 自艦が駆逐艦か巡洋艦の場合、命中率+10\%、被ダメージ-10\%

;効果をここに記述
@SKILL_0_PERSONAL_39_EFFECT(VALUE, TYPE, CLASS, CHARA_ID)
#DIM VALUE
#DIM TYPE
#DIM CLASS
#DIM CHARA_ID
#DIM STATUS, STATUS_LENGTH

IF TYPE == TYPE_命中 && (CLASS == 駆逐艦 || CLASS == 巡洋艦)
	TIMES VALUE, 1.10
ELSEIF TYPE == TYPE_被ダメージ && (CLASS == 駆逐艦 || CLASS == 巡洋艦)
	TIMES VALUE, 0.90
ENDIF
RETURN VALUE

@SKILL_0_PERSONAL_39_RATE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
RETURN 1000
