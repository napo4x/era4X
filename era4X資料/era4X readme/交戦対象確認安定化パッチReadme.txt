◇パッチの目的
参加国の設定におけるリストへの登録処理を「交戦対象になりえない様なCOUNTRYの値(0未満・MAX_COUNTRY以上)を持つ船」ではない時にのみ処理を行う様にする事で
船の持つCOUNTRYの値が意図しない値であるとしてもエラーが起きないようにすると共に、イベント戦闘などにおいて「範囲外のCOUNTRY値を設定して戦闘に参加させない」処理が可能になる様にする。


◇変更内容
SLG_COMBAT.ERB
@COMBAT 参加国の設定において 0～MAX_COUNTRYの範囲内の値の場合だけ扱う様に変更(範囲外に船を退避出来る様に)